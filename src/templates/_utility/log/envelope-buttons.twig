<div class="envelope-button">
    <button onclick="openDetails({{ envelope.envelope.id }})">Details</button>
</div>

<div class="envelope-button">
    <button onclick="openConfig({{ envelope.envelope.notificationId }})">Config</button>
</div>

<div class="envelope-button delete-envelope">
    <button onclick="deleteEnvelope({{ envelope.envelope.id }})" title="Delete Log Event">X</button>
</div>
