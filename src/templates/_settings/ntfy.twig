{% import '_includes/forms' as forms %}
{% set autosuggest = 'notifier/_form/autosuggest' %}

<h2>{{ "ntfy"|t('notifier') }}</h2>
<p>{{ "Send push notifications through [ntfy](https://ntfy.sh)."|t('notifier')|md }}</p>
<p>{{ "Consult the [{name} setup guide]({url}) for complete instructions."|t('notifier', { name: 'ntfy', url: 'https://plugins.doublesecretagency.com/notifier/getting-started/integrations/ntfy' })|md }}</p>
<p>{{ "Sensitive values can be stored in your `.env` file and referenced here."|t('notifier')|md }}</p>
<div class="pane">
    {% include autosuggest with {
        config: {
            label: "Server URL"|t('notifier'),
            instructions: "Optional, point at a self-hosted ntfy instance (if applicable). Defaults to `https://ntfy.sh`."|t('notifier'),
            name: 'ntfyServerUrl',
            placeholder: 'https://ntfy.sh',
        }
    } %}
    {% include autosuggest with {
        config: {
            label: "Access token"|t('notifier'),
            instructions: "Optional, required for protected topics or self-hosted instances with auth."|t('notifier'),
            name: 'ntfyAccessToken',
        }
    } %}
</div>

<h2>{{ "ntfy Topics"|t('notifier') }}</h2>
<p>{{ "Add the ntfy topics you want to send to. Each topic becomes available as a recipient on the **Recipients** tab when you set up a notification."|t('notifier')|md }}</p>
<div class="pane notifier-named-list">
    {{ forms.editableTableField({
        label: "Topics"|t('notifier'),
        instructions: "Click any row's **Test** button to send a quick test message to that topic."|t('notifier'),
        id: 'ntfyTopics',
        name: 'settings[ntfyTopics]',
        cols: {
            label: {
                heading: "Label"|t('notifier'),
                type: 'singleline',
                width: '30%',
            },
            topic: {
                heading: "Topic"|t('notifier'),
                type: 'singleline',
                width: '50%',
            },
            uid: {
                heading: '',
                type: 'hidden',
            },
        },
        rows: settings.ntfyTopics ?? [],
        addRowLabel: "Add a topic"|t('notifier'),
        staticRows: false,
        allowAdd: true,
        allowDelete: true,
        allowReorder: true,
    }) }}
</div>
