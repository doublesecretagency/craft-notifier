{% import '_includes/forms' as forms %}

{# status: entry.getAttributeStatus('authorId'), #}
{{ forms.elementSelectField({
    label: "Specify Message Recipients"|t('notifier'),
    instructions: "Select all users who will receive this message."|t('notifier'),
    id: 'config-recipients-specific-users',
    name: 'config[recipients][specific][users]',
    elementType: notifierUserElementType,
    selectionLabel: "Select a recipient"|t('notifier'),
}) }}
{# criteria: authorOptionCriteria, #}
{# elements: (author is defined and author ? [author]) #}
