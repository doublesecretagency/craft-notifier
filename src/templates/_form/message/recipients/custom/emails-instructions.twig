These email addresses will be the recipients of your message.

Use this snippet to generate a custom array of email addresses. You **must** use the snippet to render a JSON encoded array of email addresses.
