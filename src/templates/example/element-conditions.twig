{{ element.firstSave ? '[First Save]' }}
{{ element.isCanonical ? 'Canonical' }}
{{ element.isProvisionalDraft ? 'Provisional' }}
{{ element.isDraft ? 'Draft' }}
{{ element.isRevision ? 'Revision' }}
