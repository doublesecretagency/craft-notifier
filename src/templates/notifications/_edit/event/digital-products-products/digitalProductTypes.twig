{% if availableDigitalProductTypes()|length %}

    {% do view.registerAssetBundle('doublesecretagency\\notifier\\web\\assets\\NestedCheckboxesAsset') %}

    {% js on ready %}
    window.nestedCheckboxes.updateSelectAllLabel('.nested-checkboxes-digital-product-types');
    {% endjs %}

    <div class="digital-products-products-event-manually-triggered digital-products-products-event-date-reached digital-products-products-event-after-propagate digital-products-products-event-after-delete digital-products-products-event-after-restore hidden">
        <div class="nested-checkboxes nested-checkboxes-digital-product-types">

            <h3>Product Types</h3>
            {% if not (readOnly ?? false) %}
                <div class="select-all" onclick="window.nestedCheckboxes.selectAll('.nested-checkboxes-digital-product-types')"></div>
            {% endif %}

            <div class="instructions">
                <p>Only send notifications for selected digital product types.</p>
            </div>

            <ul>

                {# Get selected Digital Product Types #}
                {% set productTypes = (notification.eventConfig.digitalProductTypes ?? []) %}

                {# Loop over available digital product types #}
                {% for typeId,typeName in availableDigitalProductTypes() %}
                    <li>
                        <input type="checkbox"
                            class="checkbox"
                            id="digital-product-type-{{ typeId }}"
                            name="eventConfig[digitalProductTypes][]"
                            value="{{ typeId }}"
                            {{ typeId in productTypes ? 'checked=""'|raw }}
                            onclick="window.nestedCheckboxes.updateSelectAllLabel('.nested-checkboxes-digital-product-types')"
                        >
                        <label for="digital-product-type-{{ typeId }}">
                            {{ typeName }}
                        </label>
                    </li>
                {% endfor %}

            </ul>
        </div>
    </div>

{% endif %}
